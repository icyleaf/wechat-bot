version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    branches:
      ignore:
        - gh-pages
    steps:
      - checkout
      - run: bundle install
      - deploy: rake spec
